[project]
name = "asyncio-exo-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aiohttp>=3.13.2",
    "prometheus-client>=0.23.1",
    "psutil>=7.1.3",
    "uv>=0.9.15",
]
